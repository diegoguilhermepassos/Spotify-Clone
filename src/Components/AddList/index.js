import React from 'react';
import ComponentAddList from './ComponentAddList';

const AddList = ({list, setList}) => {
    return ( 
        <ComponentAddList list={list} setList={setList}/>
     );
}
 
export default AddList;