import React from 'react';
import ComponentListMusics from './ComponentsListMusics';
const ListMusics = ({list}) => {
    return ( 
        <ComponentListMusics list={list}/>
     );
}
 
export default ListMusics;