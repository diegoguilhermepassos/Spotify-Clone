import React from 'react';
import ListWithProps from './ListIcons';

const ListIcons = ({component}) => {
    return ( 
        <ListWithProps component={component}/>
     );
}
 
export default ListIcons;